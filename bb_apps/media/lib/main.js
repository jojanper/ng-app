define(['./scripts/models', './scripts/views', './scripts/routers'], function(models, views, routers) {

   ThumbnailMedia = {
       Models: models,
       Views: views,
       Routers: routers
   };

   return ThumbnailMedia;
});
