(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(['jquery', 'underscore', 'backbone', 'backbone.subroute'], factory);
    } else {
        // Browser globals
        root.Framework = factory(root.$, root._, root.Backbone, root.Backbone.Subroute);
    }
}(this, function ($, _, Backbone) {